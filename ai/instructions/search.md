# Поиск

Для поиска файлов и текста **всегда** используй MCP-инструменты `fff` вместо встроенных `Glob` и `Grep`:

| Вместо | Используй |
|--------|-----------|
| Glob | `mcp__fff__find_files` |
| Grep | `mcp__fff__grep` |
| Grep (несколько паттернов) | `mcp__fff__multi_grep` |
